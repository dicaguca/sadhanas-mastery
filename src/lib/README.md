# Library Notes

Planned shared logic:

- id helpers
- validation helpers
- date formatting
- lifecycle/status helpers
- import mapping helpers

Current files:

- `fmpImportPlan.js`
  - high-level import mapping from FMP into Sadhanas Mastery
- `fmpImportAdapter.js`
  - placeholder adapter entry points for preview + import
